class ExpenseItem {
  String name;
  double amount;
  String currency;
  DateTime dateTime;

  ExpenseItem({
    required this.name,
    required this.amount,
    required this.currency,
    required this.dateTime,
  });
}
