class ExpenseItem {
  String name;
  String amount;
  bool isExpense;
  DateTime dateTime;

  ExpenseItem({
    required this.name,
    required this.amount,
    required this.isExpense,
    required this.dateTime,
  });
}
